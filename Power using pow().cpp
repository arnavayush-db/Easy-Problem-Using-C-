#include <iostream>
#include <cmath>
using namespace std;

int main() {
    double a, b;
    cout << "Enter base and exponent: ";
    cin >> a >> b;
    cout << "Result = " << pow(a, b) << endl;
    return 0;
}
